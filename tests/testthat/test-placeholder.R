# Placeholder: real tests live in test-*.R files added per-feature.
# This file exists solely so testthat::test_local() finds at least one test file.
test_that("package loads", {
  expect_true(TRUE)
})
