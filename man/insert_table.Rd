% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/insert.R
\name{insert_table}
\alias{insert_table}
\title{Insert}
\usage{
insert_table(
  table,
  table_name_in_base,
  host = "localhost",
  port = 3306,
  db,
  user,
  password,
  chunk_size = NA,
  progress_bar = interactive(),
  ignore = TRUE,
  nolog = FALSE,
  allow.backslash = FALSE
)
}
\arguments{
\item{table}{data.frame or data.table to insert}

\item{table_name_in_base}{table in \code{db} to insert data into}

\item{host}{host}

\item{port}{port}

\item{db}{default database name}

\item{user}{user}

\item{password}{password}

\item{chunk_size}{how many elements should be inserted at a time}

\item{progress_bar}{nice progress bar to use, it's recommended to disable it in log mode}

\item{ignore}{if TRUE, uses INSERT IGNORE -- silently skips rows that violate duplicate-key/constraint rules; other errors (connection, missing table) still propagate.}

\item{nolog}{avoid any writing to the console (when TRUE, errors are not logged either)}

\item{allow.backslash}{deprecated and ignored; backslashes are now escaped correctly by DBI}
}
\value{
(invisibly) the number of rows affected (with ignore=TRUE, skipped duplicate rows are not counted).
}
\description{
Simple method that inserts the input data.frame or data.table into the designated table.
}
\details{
It's important to be aware that both input table and table in database should have the same schema (matching names, matching types).
}
\examples{
\dontrun{data <- insert_table(iris, "iris_name_in_database", host=HOST, db=DB, user=user, password=pwd)}
}
\seealso{
pull_data, selectq, insertq
}
\keyword{insert}
\keyword{mysql}
